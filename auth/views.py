from django.shortcuts import render

# Create your views here.

def home(request):
    images, - []
    return render(request, 'home.htlm', {'images': images})


def getAllImagesAndFavouritelist(request):
    images : []

    return images
